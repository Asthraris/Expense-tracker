name: expense_repository
description: "Expense Tracker Application Made by Aman Gupta(Asthraris)"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0

environment:
  sdk: ^3.8.1
dependencies:
  flutter:
    sdk: flutter
  
  firebase_auth: ^5.6.0
  cloud_firestore: ^5.6.9

  bloc: ^9.0.0
  # fl_chart: ^1.0.0
  # intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
